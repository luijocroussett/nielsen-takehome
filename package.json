{
  "name": "nielsen-car-service-center",
  "version": "1.0.0",
  "description": "take-home for Nielsen",
  "main": "index.js",
  "scripts": {
    "test": "jest --verbose",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "start": "nodemon server/server.js",
    "dev": "NODE_ENV=development nodemon server/server.js",
    "dev:clean:mock": "NODE_ENV=development START_MODE=clean MOCK_DATA=true nodemon server/server.js"
  },
  "author": "Luis Croussett",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^1.19.1",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "cookie-parser": "^1.4.4",
    "bcrypt": "^5.0.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "pg": "^7.12.0",
    "dotenv": "^8.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
